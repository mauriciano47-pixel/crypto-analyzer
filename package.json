{
  "name": "crypto-pattern-analyzer",
  "version": "1.3.0",
  "private": true,
  "scripts": {
    "build": "cd crypto-frontend && npm install && npm run build",
    "vercel-build": "cd crypto-frontend && npm install && npm run build"
  }
}
