{
  "name": "crypto-frontend",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "build": "cd crypto-frontend && npm install && npm run build && node -e \"require('fs').cpSync('dist', '../dist', {recursive: true})\""
  }
}
