{
  "version": 2,
  "buildCommand": "cd crypto-frontend && npm install && npm run build",
  "outputDirectory": "crypto-frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
